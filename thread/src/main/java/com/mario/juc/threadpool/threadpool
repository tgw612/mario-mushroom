线程池

最近在做的组件,出现使用线程池时服务响应时间过长甚至导致不可用的情况,

此处看一下线程池的配置

public ThreadPoolTaskExecutor getAsyncExecutor() {

    ThreadPoolTaskExecutor pool =

new ThreadPoolTaskExecutor();

    //当核心线程都在执行任务,则任务放入阻塞队列中

    pool.setCorePoolSize(10);



pool.setKeepAliveSeconds(60);

    //当阻塞队列已满,则新建线程去执行新的任务,直到达到最大线程数,如果达到最大线程数,任务交由饱和拒绝策略解决

    pool.setMaxPoolSize(20);



pool.setQueueCapacity(50);



pool.setThreadNamePrefix("taskExecutor- ");

    //等待线程池中的task任务执行完才shutdown

    pool.setWaitForTasksToCompleteOnShutdown(true);



pool.setAwaitTerminationSeconds(60);



pool.setRejectedExecutionHandler(new CallerRunsPolicy());



pool.initialize();

    return

pool;

}



ThreadPoolTaskExecutor为spring提供的工具类,由源码

ThreadPoolExecutor executor = new ThreadPoolExecutor(



this.corePoolSize, this.maxPoolSize, this.keepAliveSeconds, TimeUnit.SECONDS,



queue, threadFactory, rejectedExecutionHandler)

构造方法包含7个field,其中RunnableTaskQueue有以下几种:

ArrayBlockQueue:FIFO,基于数组的有界阻塞队列

LinkedBlockingQueue:FIFO,基于链表结构的阻塞队列

SynchronousQueue:不存储元素的阻塞队列,每个插入操作必须等到另一个线程调用移除操作,否则插入一直阻塞,Executors.newCachedThreadPool

PriorityBlockingQueue:

饱和策略有以下几种:

AbortPolicy:直接抛出异常

CallerRunsPolicy:只用调用者所在线程来运行任务(常用)

DiscardOldestPolicy:丢弃队列里的最近的一个任务,并执行当前任务

DiscardPolicy:不处理,丢弃



关闭线程池:

遍历线程池中工作的线程,逐个调用interrupt方法,所以无法响应中断的任务永远无法终止.

关于配置线程池线程数:

CPU密集型N(cpu)+1;

IO密集型N(cpu)*2

向线程池提交的任务可以使用execute和submit方法.

此处梳理线程池

降低资源消耗

提高响应速度,不需要重新创建线程

提高线程的可管理型此处对线程池进行监控,

taskCount:线程池需要执行的任务数量

completedTaskCount:线程池在运行过程中已完成的任务数量.

LargePoolSize:线程池中曾经创建的最大线程数量

GetPoolSize:线程池的线程数量

GetActiveCount:获取活动的线程数

此外还可以重写beforeExecute,afterExecute,terminated方法,例如监控任务的平均执行时间,最大执行时间,最小执行时间等
