常用性能监控命令

top:实时反映操作系统各个进程的资源的占用情况(包括CPU使用情况,内存使用情况,进程执行情况)
ps:查看当前进程,查看某个瞬间存在哪些进程,进程的信息和状态
ps -ef 查看所有进程
ps -ef | grep java 查看所有java进程
netstat:查看网络连接情况
netstat -a 查看当前所有连接
netstat -atl 监听tcp连接
netstat -an | grep 10090 | wc -l 统计10090端口的连接数,用来监控系统设备的IO负载情况,对系统的IO就活动进行监控
sar:性能监控 监控CPU的使用率和空闲情况,以及磁盘IO的使用情况 网卡流量的使用情况
sar -u 1 10
vmstat:虚拟内存监控
nmon:性能监控
