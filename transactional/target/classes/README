数据库事务ACID:atomic,consistency,isolation,durability
隔离性:在并发的操作下,不同的事务拥有各自的数据空间,(threadlocal),它们的操作不会对对方产品干扰.因此有不同的干扰级别,隔离级别越高,数据一致性越好,并发性越弱.
持久性:事务一旦提交,事务中得所有数据操作都必须持久化到db中.
一般采用重执行日志来保证原子性,一致性,持久性.重执行日志记录了数据库变化的每个动作.
dbms采用数据库锁机制保证事务的隔离性,当多个事务试图对相同的数据进行操作时,只有持有锁的事务才能操作数据.oracle还有数据版本机制,每个版本的读取则不影响了

并发问题如果没有相应的隔离机制就会破坏数据完整性,包括三类读问题(脏读,不可重复读,幻读)和两类数据更新问题(第一类丢失更新,第二类丢失更新)
脏读:A事务读取B事务尚未提交的更改数据,如果B事务回滚,则A事务读到错误数据
不可重复读:不可重复读是指A事务读取B事务已经提交的更改数据,则在事务提交前后,A读取相同数据不同的问题.
幻读:A事务读取B事务提交的新增数据(注意是新增而不是更改),导致事务提交前后,对数据的读取产生不一致的问题
幻读指的其他已提交事务新增的数据,不可重复读是指已提交事务更新和删除的数据,对此的防范策略是不同的,防止读到更改数据,只需添加行级锁;而防止读到新增数据,
则需要添加表级锁.(oracle则用的多版本数据实现)
第一丢失更新:A事务回滚后,把B事务的更新数据覆盖了
第二丢失更新:A事务覆盖B事务已经提交的数据,造成B事务所做操作丢失;即没有volatite特性,造成其中一个线程事务提交数据修改,其他线程不知道

数据库锁机制
行共享锁定
行独占锁定
表共享锁定
表共享行独占锁定
表独占锁定

事务隔离级别
read uncommited
read committed
repeatable read(推荐)
serializable 并发性最低 但最安全

各数据库支持的事务以及隔离级别都不同,用户可通过Connection#getMetaData()方法获取databaseMetaData对象,并通过该对象的supportsTransactions()
supportTransactionIsolationLevel(int level)查看底层数据库的事务支持






ThreadLocal的工作机制




分布式一致性

raft算法
问其他人,是否选自己,如果票数过半,则提交
paxos算法:
有一个预提交,并且每次接受到得信息如果时间晚与手上的则丢弃,如果新于自己则替换手上的成为最新,最终预提交,是否提交,真提交则选出最终结果


节点数必须为单数

BASE理论  基本可用 柔性状态 最终一致性


异步try..catch..可能捕获不到异步抛出的异 常,因为main走完后,异步才可能异常
异步的事务是可能实现的,主要针对于数据库