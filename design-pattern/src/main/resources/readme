单一职责原则:不要存在多于一个导致类变更的原因
里氏替换原则:如果对每一个类型为T1的对象t1,都有类型为T2的对象t2,使得以T1定义的所有程序P在所做对象t1都替换为t2时,程序P的行为没有发生变化,那么T2是T1得子类
依赖倒置原则:系统的高层模块不应该依赖底层模块的具体实现,二者都应该依赖其抽象类或接口,抽象接口不应该依赖具体实现类,而具体实现类应该依赖于抽象.
接口隔离原则:一个类对另一个类的依赖应该建立在最小得接口上.功能单一的接口
迪米特法则:一个对象应该对其他对象保持最少的了解
开放关闭原则:程序要对扩展开放,对修改关闭.当有新模块时,添加新的模块满足新的需求而不是修改旧模块


--适配器模式
适配器模式是解决由于接口不能兼容而导致类无法使用的问题,适配器模式会将需要适配的类转换成调用者能够使用的目 标接口.

--双亲委派模式
java虚拟机类加载器默认使用的双亲委派模式,三种默认的类加载器
bootstrap:默认加载JDK自带的rt.jar
extension:负责加载java的扩展类库,从jre/lib/ext目录下加载
system(也可称为application) classloader:从classpath环境变量中加载类文件
之后下层还有一些子类的类加载器 如tomcat提供了Common Classloader

--装饰器模式
基于组合的方式动态为对象添加功能

--建造者模式(生成器模式)
将一个复杂对象的构建过程与它的表示分离,从而使得同样的构建过程可以创建不同的表示

--模板方法模式
一个算法分为多个步骤,而这些步骤的执行次序成为一个模板方法.

--策略模式


--责任链模式
将实现逻辑拆分到多个只包含部分逻辑的功能单一的handler处理类中,码农可以根据业务需求组合不同的handler
在一条责任链中,每个Handler对象都包含对下一个handler对象的引用,一个Handler对象处理完请求消息,会把请求传给下一个handler对象继续处理.

spring中包含的设计模式
https://mp.weixin.qq.com/s/Qwz-kT2PzGGYmJ-WimDgZQ