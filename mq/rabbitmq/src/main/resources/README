<rabbitmq实战指南>
<rabbitmq实战 高效部署分布式消息队列>
<深入rabbitmq>

生产者,消费者,交换器,队列,绑定
TTL(过期时间),死信,延迟队列,优先级队列,RPC,消息持久化,生产/消费端的消息确认机制
存储机制,流控及镜像队列

主要通过交换器进行路由
支持AMQP,STOMP,MQTT协议以及几乎所有开发语言

rabbit只能把消息存在队列中,这点和kafka相反(存在topic中)
四种常用的交换器类型
fanout:将交换器里的msg路由到所有与该绑定的队列中
direct:将msg路由到Bindgkey和RoutingKey完全匹配的队列中
topic:是direct的拓展,RountingKey/BindingKey为'.'号分割的字符串,如"com.rabbit.client",BindingKey可以存在两种特殊的字符串"*"和"#"
headers:根据发送消息内容中的headers属性进行匹配,在绑定队列和交换器时指定一组键值对,当发送消息到交换器时,rabbitmq会获取到该消息的
headers,对比其中的键值对是否完全匹配队列和交换器绑定时指定的键值对,如果完全匹配则消息会路由到该队列,否则不会路由到该队列.